# CSPR.AI - Claude Tool Configuration

> Auto-generated by Smart Router on 2025-12-22
> Project: First AI-powered MCP integration for Casper Network

## Tool Usage Rules (MANDATORY)

### Research & Documentation

When you don't understand a library, need API documentation, or encounter unfamiliar code:

1. **FIRST**: Use `context7` to search official documentation for:
   - `casper-js-sdk` - Casper blockchain SDK methods and RPC client
   - `@modelcontextprotocol/sdk` - MCP server framework
   - `odra` - Smart contract framework documentation
   - React libraries and frameworks
   - Any other library you're working with

2. **THEN**: Use web search via `MCP_DOCKER` (Brave search) for:
   - Additional examples and tutorials
   - Community solutions to specific problems
   - Latest updates and migration guides

**YOU MUST do this before guessing or making assumptions.**

Example:
```
❌ DON'T: "I'll use rpcClient.getBalance() based on typical patterns"
✅ DO: "Let me search context7 for casper-js-sdk balance query methods first"
```

### Memory & Past Decisions

When context from past sessions would help, OR when making architectural decisions:

- **Search past conversations**: Use `episodic-memory:search-conversations` to find:
  - Previous architectural decisions
  - Solutions to similar problems
  - Past debugging sessions
  - User preferences and patterns

**Before making significant changes**, check if this has been discussed before.

### Code Navigation & Understanding

When exploring the codebase or navigating TypeScript/Rust code:

- **Use `serena`** for semantic code navigation:
  - `find_symbol` - Find function/class/type definitions across the codebase
  - `find_referencing_symbols` - Find all usages of a symbol
  - LSP integration for TypeScript and Rust
  - More accurate than grep for code structure

**Prefer serena over file-based searches** when looking for:
- Function definitions
- Type definitions
- Symbol references
- Call hierarchies

### Development Workflows (CRITICAL SKILLS)

These skills are **MANDATORY** workflows. Use them **BEFORE** implementing:

#### Before Writing ANY Code
- **Use `/test-driven-development`** - Write test first, watch it fail, then implement
  - Applies to: MCP tools, smart contracts, React components, ALL code
  - Ensures tests actually verify behavior
  - Prevents implementation without validation

#### Before Proposing Bug Fixes
- **Use `/systematic-debugging`** - Four-phase debugging framework
  - Phase 1: Root cause investigation
  - Phase 2: Pattern analysis
  - Phase 3: Hypothesis testing
  - Phase 4: Implementation
  - **NEVER skip directly to "here's the fix"**

#### When Writing or Modifying Tests
- **Use `/testing-anti-patterns`** - Prevent common testing mistakes:
  - Don't test mock behavior
  - Don't pollute production code with test-only methods
  - Understand dependencies before mocking
  - Keep tests maintainable

#### When Tests Are Flaky
- **Use `/condition-based-waiting`** - Replace arbitrary timeouts with condition polling
  - Eliminates race conditions
  - Fixes inconsistent pass/fail behavior
  - Waits for actual state changes

#### When Implementation is Complete
- **Use `/finishing-a-development-branch`** - Structured completion workflow
  - Reviews integration options (merge, PR, cleanup)
  - Ensures proper git workflow
  - Prevents premature commits

**These skills are NOT optional. They are mandatory development practices.**

### MCP Server Development (TypeScript)

When working on MCP tools in `mcp-server/`:

#### After Creating/Modifying MCP Tools
- **Use agent `agent-sdk-dev:agent-sdk-verifier-ts`** to verify:
  - MCP tool definitions are correct
  - Input schemas are properly defined
  - Tool descriptions are clear
  - SDK best practices are followed
  - TypeScript types are correct

#### Before Testing MCP Tools
1. Verify with agent-sdk-verifier-ts
2. Test with a real MCP client (Claude Desktop or web dashboard)
3. Check error handling for edge cases

#### TypeScript Best Practices
- Use `serena` for symbol navigation in TypeScript code
- Use `context7` for @modelcontextprotocol/sdk documentation
- Follow type safety - avoid `any` types
- Document all MCP tools with clear descriptions

### Smart Contract Development (Rust/Odra)

When working on contracts in `contracts/`:

#### Before Writing Contract Code
1. **Check Odra documentation**: Use `context7` to search for Odra patterns
2. **Apply TDD**: Use `/test-driven-development` for contract logic
3. **Review existing modules**: Check `odra-modules` for reusable components

#### Testing Contracts
```bash
# Fast iteration with OdraVM
cargo odra test

# Accurate validation with CasperVM
cargo odra test -b casper
```

- **ALWAYS test with both VMs** before considering code complete
- Use `/testing-anti-patterns` to ensure contract tests are robust
- Test edge cases: zero amounts, unauthorized access, state transitions

#### Contract Patterns to Follow
- Use `SubModule<Cep18>` for token functionality (don't reinvent)
- Use `SubModule<Ownable>` for access control
- Emit events for all state changes
- Validate inputs in entry points
- Use proper storage types: `Var<T>`, `Mapping<K,V>`, `List<T>`

#### Security Considerations
- **No private keys in code** - MCP tools build unsigned transactions
- Validate all addresses and amounts
- Check for reentrancy in token transfers
- Ensure proper access control on admin functions

### Frontend Development (React)

When working on the web dashboard in `web/`:

#### UI Components
- **Use `shadcn`** for UI components:
  - Accessible, customizable React components
  - Tailwind CSS styling
  - TypeScript support

- **Use `@magicuidesign/mcp`** for animated/special effects:
  - Enhanced UI components
  - Animations and interactions

#### CSPR.click Wallet Integration
- Use `context7` to search CSPR.click documentation
- Never store private keys
- All transaction signing happens client-side via CSPR.click
- Test with actual wallet before considering complete

#### Testing React Components
- Use browser automation (`chrome` or `playwright`) for E2E testing
- Apply `/test-driven-development` for component logic
- Test wallet connection flows manually (cannot mock wallet)

### Code Quality & Review

#### Before Committing Code

**Use these PR Review Toolkit agents proactively:**

1. **`pr-review-toolkit:code-reviewer`** - Review for:
   - Style guide adherence
   - Best practices
   - Potential bugs
   - CLAUDE.md compliance
   - **Run BEFORE every commit**

2. **`pr-review-toolkit:code-simplifier`** - Simplify code for:
   - Clarity and readability
   - Maintainability
   - Consistency
   - **Run after writing logical chunks**

3. **`pr-review-toolkit:type-design-analyzer`** - When adding new types:
   - TypeScript interfaces
   - Rust structs
   - Contract data structures
   - **Ensures strong type design**

4. **`pr-review-toolkit:silent-failure-hunter`** - After error handling:
   - Catches silently swallowed errors
   - Identifies inadequate error handling
   - Finds inappropriate fallback behavior
   - **Critical for blockchain code**

5. **`pr-review-toolkit:pr-test-analyzer`** - After creating PR:
   - Reviews test coverage completeness
   - Identifies critical gaps
   - Ensures edge cases are covered

#### Before Creating Pull Requests
1. Run all pr-review-toolkit agents
2. Ensure all tests pass (TypeScript tests, Rust tests, E2E tests)
3. Update documentation if needed
4. Use `/finishing-a-development-branch` to structure PR creation

### Web Search & Research

When you need current information or examples:

- **Use `MCP_DOCKER` Brave search** for:
  - Latest Casper ecosystem updates
  - Odra framework examples
  - casper-js-sdk usage patterns
  - MCP server examples
  - Blockchain development best practices

### Project Management

If using Asana for task tracking:
- **Use `asana`** MCP to:
  - Check task status
  - Update progress
  - Create new tasks
  - Query project roadmap

### Browser Automation & Testing

For E2E testing of the web dashboard:

- **Use `chrome`** for Chrome DevTools Protocol access:
  - Screenshots
  - DOM inspection
  - Console logs
  - Network monitoring

- **Use `playwright`** for comprehensive browser automation:
  - Cross-browser testing
  - Form filling
  - Visual validation
  - Screenshot comparison

### AI Code Search

When searching for patterns across the codebase:

- **Use `greptile`** for AI-powered code search:
  - Semantic code understanding
  - Find similar patterns
  - Code review assistance
  - Better than regex for conceptual searches

## Project-Specific Reminders

### MCP Tools Should:
- Return structured JSON responses
- Include clear error messages
- Document required permissions
- Build unsigned transactions (for write operations)
- Never handle private keys

### Smart Contracts Should:
- Follow CEP standards (CEP-18, CEP-78)
- Use Odra modules where possible
- Emit events for state changes
- Validate all inputs
- Test with both OdraVM and CasperVM

### Web Dashboard Should:
- Never store private keys
- Use CSPR.click for all signing
- Handle network errors gracefully
- Show clear transaction status
- Support testnet and mainnet switching

## Quick Reference

| When You Need | Use This |
|---------------|----------|
| **Library/API docs** | `context7` (casper-js-sdk, Odra, MCP SDK, React) |
| **Past decisions/conversations** | `episodic-memory:search-conversations` |
| **Code navigation (TypeScript/Rust)** | `serena` (find_symbol, find_referencing_symbols) |
| **Before writing ANY code** | `/test-driven-development` |
| **Before proposing bug fixes** | `/systematic-debugging` |
| **When tests are flaky** | `/condition-based-waiting` |
| **Writing/changing tests** | `/testing-anti-patterns` |
| **Implementation complete** | `/finishing-a-development-branch` |
| **After creating MCP tools** | `agent-sdk-dev:agent-sdk-verifier-ts` |
| **Before committing** | `pr-review-toolkit:code-reviewer` |
| **After writing chunks** | `pr-review-toolkit:code-simplifier` |
| **Adding new types** | `pr-review-toolkit:type-design-analyzer` |
| **After error handling** | `pr-review-toolkit:silent-failure-hunter` |
| **Before PR creation** | `pr-review-toolkit:pr-test-analyzer` |
| **React UI components** | `shadcn` or `@magicuidesign/mcp` |
| **Browser E2E testing** | `playwright` or `chrome` |
| **Web research** | `MCP_DOCKER` (Brave search) |
| **AI code search** | `greptile` |
| **Task management** | `asana` (if using Asana) |

## Critical Commands

```bash
# Smart Contracts
cd contracts
cargo odra build              # Build all contracts
cargo odra test               # Fast tests (OdraVM)
cargo odra test -b casper     # Accurate tests (CasperVM)

# MCP Server
cd mcp-server
pnpm install
pnpm dev                      # Development server
pnpm build                    # Production build

# Web Dashboard
cd web
pnpm install
pnpm dev                      # Development server

# Testing End-to-End
# 1. Start contracts on testnet
# 2. Start MCP server
# 3. Test with Claude Desktop or web UI
# 4. Use playwright for browser automation
```

## Network Endpoints

| Network | RPC URL | Explorer |
|---------|---------|----------|
| Testnet | https://node.testnet.cspr.cloud/rpc | https://testnet.cspr.live |
| Mainnet | https://node.cspr.cloud/rpc | https://cspr.live |

---

**Remember**: These are behavioral rules, not suggestions. When a tool is specified for a situation, **USE IT**. These tools exist to ensure quality, catch bugs early, and follow best practices.

This configuration was auto-generated for your CSPR.AI project. To reconfigure, run `/smart-router:init` again.
